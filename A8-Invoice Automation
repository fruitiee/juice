New Process → InvoiceBot
Variables create:
CustomerName → String
Amount → String
Email → String

Add Input Dialog
Dialog Title->"Invoice"
Input Label->"Enter name"
Value entered→ CustomerName

Add Input Dialog
Dialog Title->"Invoice"
Input Label->"Enter amount"
Value entered→ Amount

Add Input Dialog
Dialog Title->"Invoice"
Input Label->"Enter email"
Value entered→Email


Add Write Text File

File: C:\Invoices\Invoice.txt
Text:
"INVOICE" + Environment.NewLine +
"Customer: " + CustomerName + Environment.NewLine +
"Amount: " + Amount + Environment.NewLine +
"Email: " + Email

Add Message Box

"Invoice Created Successfully"
Click Run ▶
