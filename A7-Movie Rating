Step 1 — Create the project
   Open UiPath Studio
   Click New → Process
   Name it:MovieRatingBot
Click Create

create a variable names as  moviename--datatye-string
                            movierating--datatype-string

Step 2 — Create Input Dialog
In Activities, search:Input Dialog
    dialog title:"movie"
    input label:"enter movie name"
    value entered:moviename
go to properties of input dialog---Result-moviename

Open imdb website >search for tamil movies

drag use application browser>indicate the screen(imdb)
do>get text>indicate the imdb screen--rating(8.0)
drag message box inside the do of application browser
 text--"Inception Rating: " + movierating
drag another messge box inside the do of application browser
  text--"Movie: " + movieName + vbCrLf + "IMDb Rating: " + movierating
